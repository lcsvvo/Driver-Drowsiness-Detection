# 운전자 졸음 감지 — 실행에 필요한 패키지
#
# 버전은 상한 없이 하한만 둔다. 개발 환경은 아래와 같았고, 그보다 낮으면
# 안 되는 이유가 있는 것만 하한을 박았다.
#
#   Python 3.12.4 / TensorFlow 2.21.0 / OpenCV 5.0.0
#   NumPy 2.5.1 / pandas 3.0.3 / matplotlib 3.11.0
#
# 설치:  pip install -r requirements.txt

# cv2.FaceDetectorYN (YuNet) 이 4.5.4 에서 들어왔다. 이보다 낮으면
# 02_INFER_YuNet.ipynb 이 두 번째 셀에서 바로 멈춘다. 가장 흔한 실패 지점이다.
opencv-python>=4.5.4

# 가중치가 Keras 3 의 .keras 포맷으로 저장돼 있다.
tensorflow>=2.16

numpy>=1.24
pandas>=2.0
matplotlib>=3.7

# 입 벌림 게이트에서 FaceLandmarker Tasks API를 사용한다.
# model/detectors/face_landmarker.task 파일은 별도로 받아야 한다.
mediapipe

# 노트북 실행용. VS Code 에서 커널을 잡으려면 ipykernel 이 필요하다.
ipykernel>=6.25
ipython>=8.12
