# Driver Drowsiness Detection — 실행 의존성
#
# 저장소 루트에 이 파일이 있어야 노트북 부트스트랩 셀이 저장소 루트를 식별한다
# (config.py 와 requirements.txt 를 '둘 다' 가진 폴더만 루트로 인정 — STYLE_GUIDE 템플릿).
# 버전은 상한을 두지 않는다. 팀원 4명의 Python 버전(3.10~3.12)이 달라
# 하드 핀을 걸면 설치가 깨진다.

# --- 핵심 ---
numpy>=1.24
opencv-python>=4.8          # FaceDetectorYN(YuNet) 은 4.5.4+ 필요
pandas>=2.0
tensorflow>=2.13            # Windows 네이티브는 2.10 이후 GPU 미지원 → CPU 학습

# --- 평가·시각화 ---
scikit-learn>=1.3           # classification_report, confusion_matrix, roc_auc_score
matplotlib>=3.7

# --- 노트북 실행 ---
ipykernel>=6.25
nbformat>=5.9

# --- 데이터 취득 (01 baseline 전용) ---
kagglehub>=0.2

# --- 하품 트랙(팀원) 전용 ---
# mediapipe>=0.10           # yawn_mar_validate.py. 눈 트랙에는 필요하지 않다.
